# wrk2 (Cross-Validation)

This directory is used for `wrk2` to cross-validate open-loop load generation results.

To clone and compile `wrk2`:

```bash
git clone https://github.com/giltene/wrk2.git source
cd source
make
cp wrk ../wrk2
```
